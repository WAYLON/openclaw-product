# 平台安装教程

> 目标：先按官方方式装好 OpenClaw 本体，再把本仓库作为当前 7 个首发 Agent 的交付工程接入本机环境。

当前平台口径：

- 运行态：`main + 6` 个业务 Agent
- 默认模型：`local-proxy / gpt-5.4`
- 飞书：`7` 个 bot 对 `7` 个 Agent

## 1. 安装 OpenClaw

安装口径：

- OpenClaw 本体先看官方文档
- 飞书插件先看官方文档
- 本文只保留当前项目已验证可用的官方命令入口

```bash
npm install -g openclaw
openclaw onboard --mode local --flow quickstart --non-interactive --accept-risk --install-daemon --skip-channels --skip-search --skip-ui --skip-health --auth-choice skip
npx -y @larksuite/openclaw-lark install
```

验证：

```bash
openclaw --version
openclaw status --deep
```

## 2. 拉取项目

```bash
cd ~/Desktop
git clone https://github.com/WAYLON/openclaw-product
cd openclaw-product
```

## 3. 初始化项目工具链

```bash
python3 -m venv .venv
source .venv/bin/activate
./agent-platform init
./agent-platform doctor
```

## 4. 当前首发角色

- `main`
- `education-agent`
- `stock-agent`
- `loan-agent`
- `social-media-agent`
- `news-agent`
- `sales-agent`

## 5. 当前共享技能

- `web-access`
- `opencli`
- `self-improving-agent`
- `skill-vetter`
- `skill-creator`
- `summarize`
- `agent-browser`
- `nano-pdf`
- `nano-banana-pro`
- `excel-xlsx`
- `word-docx`
- `desktop-control`

## 6. 当前专有技能

- `stock-agent`
  - `tradingagents-analysis`
- `education-agent`
  - `openmaic`

## 7. 关键规则

- 运行时 key 写 LaunchAgent
- 配置型 skill key 写 `openclaw.json`
- 一个飞书 bot 只绑定一个 Agent
- 群聊只在 `@机器人` 时回复
- 安装动作优先以官方文档为准，本项目文档只补当前交付结构
