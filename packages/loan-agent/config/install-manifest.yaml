package:
  id: loan-agent
  display_name: 助贷专业包
  language: zh-CN
  summary: 线索采集、资格预检、材料引导、预约与合规流程交付
  install_mode: parallel_agent_package
  requires:
    platform_initialized: true
    docs_complete: true
  includes:
    - skills/
    - docs/quickstart.md
    - docs/user-guide.md
    - docs/trainer-guide.md
  # 当前专业包可先安装；表单、CRM、预约、签署与 OCR 凭证按需初始化。
  required_keys: []
  supports:
    independent_install: true
    independent_enable: true
    independent_upgrade: true
    independent_offline: true
